<% if product = Product.find_by(id: product_id) %>
  <% item_total = product.price * quantity %>
  <% @order_total += item_total %>
   <p><%= product.name %>:     $<%= product.price %> x <%= quantity %> = $<%= item_total %></p>
<% end %>